-- ============================================================
-- Job2804 덴트잡 서울경기 — 스키마 v7 (급여 협의 가능)
-- schema.sql ~ schema_v6.sql 적용 후 이어서 실행하세요.
-- ============================================================

-- pay_min을 null 허용으로 변경 — null이면 "급여 협의(비공개)"로 표시합니다.
alter table job_posts alter column pay_min drop not null;
